import { PlayerStatPipe } from './player-stat.pipe';

describe('PlayerStatPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerStatPipe();
    expect(pipe).toBeTruthy();
  });
});
